`?/home/matthew/Projects/lycan2/hdl/test/simple_module.svpy
``line `empy.getContext().line "`empy.getContext().name" 0
`{myvar = 4}`
``line `empy.getContext().line "`empy.getContext().name" 0
// myvar = `myvar` , that's pretty cool

module simple_module(
`[for i in range(myvar)]`
``line `empy.getContext().line "`empy.getContext().name" 0
    input in`i,
`[end for]`
`[for i in range(myvar)]`
``line `empy.getContext().line "`empy.getContext().name" 0
    output outp`i,
`[end for]`
);

endmodule
